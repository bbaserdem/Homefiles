# Change default library location
# <I switched to using R_LIBS
#myPaths = .libPaths()
#myPaths[1] = gsub("sbp/", "sbp/Data/", myPaths[1])
#.libPaths(myPaths)

# Save history in cache folder
#savehistory(file = "/home/sbp/.cache/R/Rhistory")
